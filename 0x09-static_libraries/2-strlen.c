#include "main.h"
#include <string.h>

/**
 * _strlen - entry point
 * Return: length of input string
 * description: returns the length of a string
 * @s: a pointer value that constains a string
*/


int _strlen(char *s)
{

return (strlen(s));

}
