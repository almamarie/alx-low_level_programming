#include "main.h"
#include <ctype.h>

/**
 * _isupper - entry point
 * Return: 1 if is upper, 0 otherwise
 * @c: integer input
 * Description: checks if the input is upper
*/

int _isupper(int c)
{
return (isupper(c));
}
