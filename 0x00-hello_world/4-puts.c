#include<stdio.h>

/**
 * main - Entry pount
 * Return: Always 0 (success)
 * This code prints a string to stdout using puts()
 */

int main(void)
{
puts("Programming is like building a multilingual puzzle");
return (0);
}
